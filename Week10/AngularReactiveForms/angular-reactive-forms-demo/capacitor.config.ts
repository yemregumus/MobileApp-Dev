import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-reactive-forms-demo',
  webDir: 'www'
};

export default config;
