import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'data-persistence-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
